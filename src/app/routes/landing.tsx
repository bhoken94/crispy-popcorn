const LandingRoute = () => {
  //   const handleStart = () => {
  //     if (user.data) {
  //       navigate(paths.app.dashboard.getHref());
  //     } else {
  //       navigate(paths.auth.login.getHref());
  //     }
  //   };

  return (
    <>
      <h1>Landing</h1>
    </>
  );
};

export default LandingRoute;
